# Workspace Rules

- **Package Manager**: Always use `bun` instead of `npm` for all package management and script execution tasks in this project (e.g. `bun run build`, `bun run dev`, `bun install`).
